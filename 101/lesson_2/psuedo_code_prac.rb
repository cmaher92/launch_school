# Pseudo-code practice

# Write a method that returns the sum of two ints

START

method sum two ints
  PRINT int1 + int2

END

# Write a method that takes an array of strings, returns a string that is all of those strings concatenated together
START

def concatenates_strings(array_of_strings)
    PRINT array_of_strings.join


END

# Write a method that takes an array of ints, and returns a new array with every other element

START

def every_other(array_of_ints)
    every_other = map array_of_ints
       return only ints that are odd
       

END